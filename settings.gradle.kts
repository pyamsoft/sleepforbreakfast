pluginManagement {
  repositories {
    gradlePluginPortal()
    google()
    mavenCentral()
  }
}

rootProject.name = "SleepForBreakfast"

include(":app")

include(":category")

include(":core")

include(":db")

include(":db-room")

include(":home")

include(":main")

include(":money")

include(":repeat")

include(":spending")

include(":transactions")

include(":ui")

include(":worker")

include(":worker-workmanager")
